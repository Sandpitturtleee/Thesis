from Data.generate_graphs import generate_graphs

if __name__ == '__main__':
    #random_graph = generate_random_graph(1000)
    generate_graphs()
    # save_graph_to_json(graph, "1000.json")
    # loaded_graph = load_graph_from_json("1000.json")
    # print(loaded_graph)

    # Pokaż kawałek grafu:
    #print(dict(list(random_graph.items())[:5]))  # wyświetli 5 pierwszych wierzchołków
